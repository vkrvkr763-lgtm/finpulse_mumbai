import random

def run_quantum_optimizer(portfolio: list):
    # Mock quantum optimizer output for demo
    return {
        "Hydrogen": round(random.uniform(5, 15), 2),
        "Solar": round(random.uniform(15, 30), 2),
        "Wind": round(random.uniform(20, 35), 2)
    }
